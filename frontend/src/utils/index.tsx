import { totalPrice, totalAmount } from "./calcs";
import { parseLocalCurrency, parseAlt, urlReplace } from "./formatting";
import { waitingProduct } from "./isWaiting";
import { stickyVariant, productDetails, findInArray, } from "./helpers";
import { slideOptions } from "./templates"
export { totalAmount, slideOptions, productDetails, urlReplace, stickyVariant, totalPrice, findInArray, parseLocalCurrency, waitingProduct, parseAlt }